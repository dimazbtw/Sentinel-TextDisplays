<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>github.dimazbtw</groupId>
    <artifactId>sentinel-textdisplays</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Sentinel-TextDisplays</name>
    <description>Per-player Text Display entities with PlaceholderAPI, ItemsAdder/Nexo, HEX colors, scaling and click interactions (1.19.4+)</description>

    <properties>
        <!-- Compile to Java 17 bytecode so the plugin runs on Java 17, 18, 19, 20 and 21 -->
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <packetevents.version>2.7.0</packetevents.version>
    </properties>

    <build>
        <finalName>${project.name}-${project.version}</finalName>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
        </plugins>

        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>

    <repositories>
        <repository>
            <id>spigotmc-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
        <repository>
            <id>sonatype</id>
            <url>https://oss.sonatype.org/content/groups/public/</url>
        </repository>
        <repository>
            <id>codemc-repo</id>
            <url>https://repo.codemc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Spigot API 1.20.1 as the compile base (only plugin/command/event/config API used) -->
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>1.20.1-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>

        <!--
          PacketEvents is NOT bundled. The server must run the standalone PacketEvents
          plugin (declared as a hard dependency in plugin.yml); we compile against its API.
          This also pulls in adventure-api transitively for compilation; at runtime the
          Component classes are provided by the PacketEvents plugin.
        -->
        <dependency>
            <groupId>com.github.retrooper</groupId>
            <artifactId>packetevents-spigot</artifactId>
            <version>${packetevents.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>
