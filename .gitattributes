# Normalize all text files to LF in the repository
* text=auto eol=lf

# Windows scripts must keep CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# Unix scripts must keep LF (the Maven wrapper runs on Linux CI)
mvnw text eol=lf
*.sh text eol=lf
